import { adminDb } from '../config/supabase.js'
export async function metrics(){const db=adminDb();const count=async query=>{const {count,error}=await query;if(error)throw error;return count||0};return {total_users:await count(db.from('users').select('*',{count:'exact',head:true})),total_influencers:await count(db.from('users').select('*',{count:'exact',head:true}).eq('role','influencer')),total_brands:await count(db.from('users').select('*',{count:'exact',head:true}).eq('role','brand')),conversations:await count(db.from('conversations').select('*',{count:'exact',head:true})),active_users:await count(db.from('users').select('*',{count:'exact',head:true}).eq('is_disabled',false)),new_registrations:await count(db.from('users').select('*',{count:'exact',head:true}).gte('created_at',new Date(Date.now()-7*864e5).toISOString()))}}
export async function users({role,query}){let q=adminDb().from('users').select('id,name,email,role,is_disabled,created_at').order('created_at',{ascending:false}).limit(100);if(role)q=q.eq('role',role);if(query)q=q.or(`name.ilike.%${query}%,email.ilike.%${query}%`);const {data,error}=await q;if(error)throw error;return {items:data}}
export async function updateUser(id,payload){const update={};if(typeof payload.isDisabled==='boolean')update.is_disabled=payload.isDisabled;if(typeof payload.profileStatus==='string')update.profile_status=payload.profileStatus;const {data,error}=await adminDb().from('users').update(update).eq('id',id).select('id,name,email,role,is_disabled').single();if(error)throw error;return data}
