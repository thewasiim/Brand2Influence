import { adminDb } from '../config/supabase.js'
import { ApiError } from '../utils/api-error.js'
export async function getMe(auth){const {data,error}=await adminDb().from('users').select('id,name,email,role,is_disabled,created_at').eq('id',auth.id).maybeSingle();if(error)throw error;if(data?.is_disabled)throw new ApiError(403,'This account is disabled','ACCOUNT_DISABLED');return data||{id:auth.id,name:auth.name||auth.email?.split('@')[0]||'',email:auth.email,role:null}}
export async function setRole(auth,role){if(!['brand','influencer'].includes(role))throw new ApiError(400,'Role must be brand or influencer','VALIDATION_ERROR');const db=adminDb();const {data:existing,error:existingError}=await db.from('users').select('role,name').eq('id',auth.id).maybeSingle();if(existingError)throw existingError;if(existing?.role==='admin')throw new ApiError(403,'Admin roles are assigned securely','FORBIDDEN');const {data,error}=await db.from('users').upsert({id:auth.id,email:auth.email,name:existing?.name||auth.name||auth.email?.split('@')[0]||'New user',role},{onConflict:'id'}).select('id,name,email,role').single();if(error)throw error;return data}
